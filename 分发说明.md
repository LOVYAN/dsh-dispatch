# dsh-dispatch 怎么分享给别人

不能把 Tailscale / ntfy App「塞进一个 zip」。那是别人设备上的独立软件，有各自商店、签名和账号。能打进包的只有 **DSH 插件 + 安装脚本 + 文档**。

```
你发给对方的 zip
├── plugin/                 DSH 宿主插件（唯一自研代码）
├── install.ps1             一键装到对方电脑的 DeepSeek Harness
├── docs/                   手机端怎么配
└── 分发说明.md             本文件
```

对方电脑还要自己有的（不打包）：

| 东西 | 必装？ | 谁提供 |
|---|---|---|
| DeepSeek Harness | 是（电脑） | 官方安装包 |
| Tailscale（PC + 手机同一账号） | **是，这是唯一必装的第三方** | tailscale.com |
| ntfy App | **否**。只要锁屏推送才装 | Play / F-Droid |

默认路径：**电脑 DSH + 手机 Tailscale + 系统浏览器打开会话页**。审批、看回复、续聊都在这一页完成，不必装 ntfy。

## 你怎么发出去

在本目录执行：

```powershell
pwsh -File pack.ps1
```

得到 `dist\dsh-dispatch-日期.zip`。把这个 zip 丢网盘 / 微信即可。  
**不要**把 `.dsh-home\profiles\web\cordis.patch.yml` 一起发——里面是你的 token 和主题名。

## 对方电脑怎么装

1. 已能打开 DeepSeek Harness Web（`http://127.0.0.1:3080`）。
2. 解压 zip，在解压目录执行：

   ```powershell
   pwsh -File install.ps1
   ```

3. 脚本会：
   - 把插件拷进 `.dsh-home\profiles\web\node_modules\dsh-dispatch`
   - 若 `cordis.patch.yml` 里还没有本插件，就追加一段（**现场生成** token 和随机主题，不复用你的）
   - 若已装 Tailscale，尝试 `tailscale serve --bg --https=443 http://127.0.0.1:3080`
4. **重启** DeepSeek Harness。
5. 浏览器打开 `http://127.0.0.1:3080/dispatch/health` 应看到 `{"ok":true,...}`。

## 对方手机怎么配（推荐：只装 Tailscale）

1. 装 [Tailscale](https://tailscale.com/download/android)，登录**和电脑同一个账号**。电池 → 无限制，允许自启动。
2. 手机浏览器打开脚本打印的会话页：

   `https://<电脑.ts.net>/dispatch/chat?token=<对方自己的token>`

看回复、续聊、批准/拒绝都在这一页。会话卡住需要审批时，页顶会出现横幅按钮。

可选：再装 [ntfy](https://ntfy.sh) 订阅脚本打印的主题，锁屏才会主动弹。不装也能用，只是不会震你。

## 三种分享强度（按你想给谁）

| 模式 | 给什么 | 适合 |
|---|---|---|
| A. 插件包（推荐） | 上面的 zip，对方自己跑 `install.ps1` | 朋友也有 DSH，想在自己电脑上用 |
| B. 远程帮你批 | 不发插件。让对方加入**你的** tailnet，订阅**你的** ntfy 主题 | 你电脑干活，别人只帮忙点批准（权限等于把审批权交给他） |
| C. 做成 DSH 官方插件形态 | 以后把 `plugin/` 发到独立 git 仓库，用 `pnpm` 当依赖 | 你要长期维护、给很多人用 |

B 不要轻易给人：decision URL 里带着 token，拿到主题 ≈ 能替你批权限。

## 明确做不到 / 不该做的

- 把 Tailscale / ntfy 的 MSI、APK、exe 推进 GitHub：许可证不允许再分发，仓库会被 DMCA / 体积爆掉。克隆后跑 `vendor/fetch-official.ps1` 从官方源下。
- 让对方「只装一个 App 就完事」：DSH 在 PC 上，手机只是遥控器。除非有一天官方出手机客户端。
- 自建 ntfy 打进包：GitHub 发行包在你这环境经常下不到；`install.ps1` 默认用 ntfy.sh 公共服务。
- 把本机 `cordis.patch.yml` 里的 token / 主题写进仓库或安装脚本：那是这台机器的钥匙。

## 更新插件

改完 `plugin/lib/index.js` 后：

- 你自己：`pwsh -File deploy.ps1` 然后重启 DSH  
- 给别人：再跑一遍 `pack.ps1`，发新 zip；对方覆盖解压后再跑 `install.ps1` 并重启
